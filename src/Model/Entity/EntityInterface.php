<?php

namespace IdealOctoTelegram\Model\Entity;

/**
 * An interface for objects that will be stored/retrieved from a Source.
 * Nothing really to put here for now. We use this to inditicate that the
 *   sources are agnostic to the type of object they actually store, as long
 *   as the object implements this interface.
 */
interface EntityInterface {}